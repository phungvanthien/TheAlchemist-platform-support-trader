import { PublicKey, Keypair } from '@solana/web3.js';
import { ParimutuelWeb3 } from '../web3';
/**
 * send mint token
 * @param walletPubkey user wallet pubkey where token will send to
 * @param mintPubkey mint token pubkey
 * @param authorityKeypair keypair that owns mint token
 * @param amount amount to send
 * @returns void
 */
export declare function sendTransferTokenTransaction(this: ParimutuelWeb3, walletPubkey: PublicKey, mintPubkey: PublicKey, authorityKeypair: Keypair, amount: number): Promise<void>;
