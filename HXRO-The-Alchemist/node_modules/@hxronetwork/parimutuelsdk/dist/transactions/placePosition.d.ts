import { Keypair, PublicKey } from '@solana/web3.js';
import { ParimutuelWeb3 } from '../web3';
import { PositionSideEnum, WalletSigner } from '../types';
export declare function sendPlacePositionTransaction(this: ParimutuelWeb3, userWallet: WalletSigner | Keypair, parimutuelPubkey: PublicKey, size: number, side: PositionSideEnum, seq: number): Promise<string>;
