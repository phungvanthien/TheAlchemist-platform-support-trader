import { MintData } from '../types';
export declare const getMintDecimals: (mints: MintData[], cryptoAddress: string) => number;
export declare const shortenAddress: (address: string, chars?: number) => string;
